name = input("Input your name: ");
# gets input from user. shows prompt (stuff inside the parenthesis) and stores user input inside "name"
age = input("Input your age: ");

print("Hello " + name + "!");
# prints the name gotten from user

print(f"You are {age} years old!");