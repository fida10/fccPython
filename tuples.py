someTuple = ("Hell", "Heaven", "God");
# tuple initialized with "()" as opposed to lists which start with "[]"

print(someTuple[0]);
# prints element at index 0 (position 1) within the tuple
# elements within are called just like with lists, using "[]"


# someTuple[1] = "Angels";
# this line will give an error because tuples are immutable, cannot be changed after initialization

listOfTuples = [("One", "Tuples"), ("Two", "Tuples"), ("Three", "Tuples")];
# a list of tuples!

